# Base URL for live FastAPI Backend service
VITE_API_BASE_URL=http://localhost:8000/api/v1

# When true, uses the high-fidelity in-memory calculation & simulation engine
# even if backend is offline, guaranteeing a 100% working live demo
VITE_USE_MOCK=true
